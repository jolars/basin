[project]
description = "Ad-hoc tooling for basin (paper ingestion etc.). Not published."
name = "basin-tools"
version = "0"
requires-python = ">=3.10"
dependencies = ["cma"]

[tool.uv]
package = false
