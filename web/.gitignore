node_modules/
.svelte-kit/
build/
.vite/

# wasm-pack output: regenerated by `pnpm run build:wasm`. Keep the
# directory tracked via .gitkeep so $lib/basin-wasm imports resolve in
# editors before the first build.
src/lib/basin-wasm/*
!src/lib/basin-wasm/.gitkeep
